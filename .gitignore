# Python
__pycache__/
*.pyc
.venv/

# dataset downloads + generated tabular data: redistribution is restricted by
# each study's data use agreement, and it's large -- never git-tracked.
# See AGENTS.md / README.md for the data/<DATASET>/{search,study,docs,tabular}
# layout; keep the data itself local (or on a separately-managed backup).
data/
legacy/csv_exploration/PPMI_explo/*.csv
logs/
tmp/
qc_results_save/
qc_outliers/
quality_control/qc_results/

# local/machine-specific
.ppmi_config
processing_old
.~lock*
